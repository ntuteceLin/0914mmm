# 0914mmm

let idolAge = 25;
let idolHeight = 185;
console.log("Age: " + idolAge);
console.log("Height: " + idolHeight);

let idolName = "DDD";
let introMessage = "Hello everyone, I'm " + idolName + "! Please support me.";
console.log(introMessage);


let isOnStage = true;
    if (isOnStage) 
        {
            console.log(idolName + " is performing on stage now!");
        } 
    else 
        {
            console.log(idolName + " has no performance today.");
        }

let idolFanclubName;
console.log(idolFanclubName);

let idolPet = null;
    if (idolPet == !null)
        {
            console.log(idolName + " doesn't have a pet.");
        } 
    else 
        {
            console.log(idolName + " has a pet: " + idolPet + ".");
        }
        
